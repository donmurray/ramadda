<?xml version="1.0" encoding="UTF-8"?>


<project basedir="." default="ramadda" name="RAMADDA">

    <target name="init">
        <property name="distdir" value="${basedir}/dist"/>
        <mkdir dir="${distdir}"/>

        <property name="dist_javadoc" value="${distdir}/javadoc"/>
        <mkdir dir="${dist_javadoc}"/>

        <property name="lib" value="${basedir}/lib"/>
        <property name="srcdir" value="${basedir}/src"/>

<!--
used by the javadoc target
-->
        <property name="classpath" value="${lib}/servlet-api.jar:${lib}/unidatacommon.jar:${lib}/idv.jar:${lib}/repository.jar"/>

    </target>



<target name="commoninit">
      <mkdir dir="${distdir}"/>
</target>



    <target name="plugin" depends="init,clean">
        <echo message="Building plugin ${plugindir}"/>
        <javac
            classpath="${classpath}"
            verbose="false"
            debug="true"
            deprecation="false" 
            nowarn="true"
            srcdir="${srcdir}"
            failonerror="${javac.failonerror}"
            fork="${javac.fork}"
            memoryMaximumSize="${javac.maxmemory}"
            target="1.5"
        >
        <include name="${plugindir}/*.java"/>
        </javac>

        <echo message="Making jar ${distdir}/${jarfile}"/>
        <jar 
            basedir="${srcdir}"
            update="false"
            compress="true"
            jarfile="${distdir}/${jarfile}">
            <include name="${plugindir}/**/*.class"/>
            <include name="${plugindir}/htdocs/**/*"/>
            <include name="${plugindir}/*.xml"/>
            <include name="${plugindir}/*.properties"/>
	    </jar>
    </target>



    <target name="plugin_install">
        <copy overwrite="true"  todir="${user.home}/.unidata/repository/plugins">
             <fileset file="${distdir}/${jarfile}"/>
        </copy>
    </target>




   <target name="clean" depends="init" >
        <delete>
            <fileset dir="${srcdir}/org" includes="**/*.class"/>
        </delete>
    </target>


   <target name="purge" depends="init,clean" >
     <delete dir="${distdir}"/>
    </target>


    <target name="ramadda" depends="init">
      <echo message="Building RAMADDA"/>
      <ant antfile="${srcdir}/ucar/unidata/repository/build.xml" inheritAll="false"/>
    </target>

    <target name="plugins" depends="init">
      <echo message="Building RAMADDA plugins"/>
      <ant antfile="${srcdir}/org/ramadda/build.xml" inheritAll="false"/>
      <ant antfile="${srcdir}/org/ramadda/wms/build.xml" inheritAll="false"/>
      <ant antfile="${srcdir}/org/ramadda/ontology/build.xml" inheritAll="false"/>
    </target>



    <target name="javadoc" depends="init">
        <javadoc
            Author="true"
            Doctitle="RAMADDA"
            Use="true"
            Version="true" 
            Windowtitle="RAMADDA"
            classpath="${classpath}"
            destdir="${dist_javadoc}"
            maxmemory="512m"
            sourcepath="${srcdir}"
                 >
            <link href="http://java.sun.com/javase/6/docs/api/"/>
            <package name="ucar.unidata.ramadda.*"/>
<!--
            <excludepackage name="org.unavco.???"/>
-->
        </javadoc>
    </target>



</project>
